spring:
  application:
    name: front
server:
  port: 3000
eureka:
  instance:
    hostname: localhost
  client:
    serviceurl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
      fetchRegistry: false
